{
    "name": "@writools/wagon-forms",
    "version": "1.0.0-beta.4",
    "description": "",
    "license": "MIT",
    "repository": "https://github.com/writools/wagon-forms.git",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "build": "tsc",
        "test": "jest --colors",
        "prebuild": "rimraf dist",
        "clean": "rimraf dist",
        "buildAndPublish": "npm run build && npm publish --access public"
    },
    "dependencies": {
        "@writools/wagon-ext": "^1.0.0",
        "rxjs": "^6.6.3"
    },
    "devDependencies": {
        "@types/jest": "^26.0.20",
        "jest": "^26.6.3",
        "jest-extended": "^0.11.5",
        "rimraf": "^3.0.2",
        "ts-jest": "^26.5.1",
        "typescript": "^4.1.5"
    }
}
